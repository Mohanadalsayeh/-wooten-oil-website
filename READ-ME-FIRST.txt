Deploy these changed portal files to the existing project. Redeploy the Worker/backend so fleet/intevacon.mjs is live; worker.js is unchanged and intentionally omitted. Then install PC v1.9.

Admin Fleet Cards & Transactions now shows collection/publishing progress, last successful pull with Central Time date and time and counts, and the latest failure reason and time. The panel refreshes every 30 seconds while open. Last good data remains available after failure. After four hours without a report, it shows an overdue notice: the PC may be off or unreachable; the exact cause cannot be known remotely. No email/SMS alerts are added.

The new fleet_health table is created automatically; no manual migration required. Failure messages use approved reason categories plus a safe browser error code. Customer API responses do not expose admin status or Billable Amount.

Verification: 12 backend tests passed including atomic publication, account isolation and pre-upload failure reporting. UI text/state/escaping checks passed. Browser visual rendering could not be run in this environment because Chromium is not installed.
