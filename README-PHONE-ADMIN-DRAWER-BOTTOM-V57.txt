WOOTEN OIL PORTAL — PHONE ADMIN DRAWER BOTTOM FIX V57

Changes:
- Uses the phone browser's live visible viewport height for the admin drawer.
- Updates drawer height when Safari's browser bars change size.
- Adds protected scrolling clearance below the last navigation item.
- Keeps Admin Users fully visible above Safari's bottom toolbar.
- Preserves the version 56 close-button correction.

All prior sidebar, dashboard, permissions, and portal functions remain unchanged.
