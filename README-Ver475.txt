Replace four included files and deploy Pages AND Worker (fleet/intevacon.mjs changed). Fleet header sorting now uses server-side ordering before pagination. Applies to admin/customer cards and transactions, preserves filters, resets to page one on sort, toggles direction, and carries ordering into refresh and admin PDF export. Numeric amounts sort numerically. No PC agent update needed.
