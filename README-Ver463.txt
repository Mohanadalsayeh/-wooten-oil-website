Replace the three included files in the existing portal.
Admin Transactions now shows all 18 checked source fields as separate columns, plus Portal account. Scroll the table horizontally to see every column.
Customer presentation is unchanged; Billable Amount stays admin-only.
Existing PDF export retains all checked fields in its grouped print layout.
Requires the v2.2 checked-column sync agent to populate the newly collected fields. Existing missing values display a dash until synced.
No Worker or PC agent change in this update.
