Apply over Ver457, preserving folder paths. Deploy BOTH the frontend and the Worker with the updated fleet/intevacon.mjs dependency. worker.js is unchanged and is not included. New control table is created automatically.

Adds Sync now and a schedule selector (2,4,6,...24 hours) for administrators with Fleet access. Credential creation/revocation remains owner-only. Saving the interval schedules the next automatic pull from the time of saving. A manual pull resets the next automatic pull to the selected interval after its start. The first PC check starts a pull immediately unless a future schedule has already been saved. Offline manual requests remain queued. The UI displays queued/running status, next due time in Central Time, and PC check-in status. Collection success/failure and automatic table refreshing are retained.

REQUIRED PC UPDATE: Apply Intevacon-Portal-Control-v2.0.zip to the EXISTING sync folder when no sync is running. Run Enable-Schedule.cmd once on that PC under the existing Windows user. This replaces the existing two-hour task with a one-minute request check; it does NOT pull Intevacon data every minute. Keep the PC on, connected and Windows signed in. Do not create new credentials. A normal browser may briefly appear before minimizing, as in the previous working version.

Validation: 13 backend tests passed, including schedule bounds, authorization, queue deduplication, lease ownership and expiration. Python poll tests passed and JavaScript syntax checked. Windows Task Scheduler and authenticated live-site execution could not be tested here.
