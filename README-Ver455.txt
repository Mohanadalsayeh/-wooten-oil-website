Wooten Oil portal Ver455

Apply these four files over the current Ver454 portal, preserving their folders, then deploy using your existing process. No Worker or database changes.

Both admin and customer Fleet Cards / Transactions use the shared Credit & Collections pagination styling, including page numbers, Previous 20, Next 20, selected state and responsive layout.
Admin Fleet Cards and Transactions have dedicated PDF exports using all records matching the current search/review filter. Card numbers remain bold. The exporter stops with a retry message if the sync changes during collection. Changing tabs, pages or filters cancels collection.

Validated JavaScript syntax and generated PDFs for both datasets with bold card numbers. Live browser and authenticated production checks were not available. After deploying, refresh both pages and check both admin Export PDF buttons.
