Ver550 — Apply over Ver549.
Replace admin-customers.html and worker.js, deploy the Worker, then refresh the admin page.
Without email turns Email PDF off and disables it for both cycles. With email or All customers makes the checkbox available again. Check it to enable email delivery. The server also enforces this rule for saved settings and scheduled runs.
