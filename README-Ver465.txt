Replace these three files in your existing portal.
Fleet card and transaction headers retain readable column widths, even with no matching records.
Sorting labels align left, arrows right. Rounded button boxes and hover movement are removed from headers.
Applies to admin and customer Fleet tables. Horizontal scrolling stays inside the table.
No Worker or PC agent update required.
