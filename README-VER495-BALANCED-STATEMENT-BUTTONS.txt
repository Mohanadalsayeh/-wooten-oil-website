Wooten Oil Portal - Ver495

Applies Style 1, Balanced Grid, to Send Account Statements.

Desktop/tablet:
  Row 1: Load Customers | Export PDF
  Row 2: Preview Statements | Generate & Send Statements

The four main buttons have equal widths and heights. Preview is navy, Send is
red, and the utility buttons are pale gray. On phones, all four buttons stack
at equal width and height. The selection count appears beside Statement actions.
Preview PDF links are grouped in one compact row, and selection controls appear
below a divider.

Copy the included files into the existing project, keeping their paths, and
deploy the website. Requires the statement preview functionality from Ver494.
No Worker update or new settings are needed for Ver495.

Included changed files:
  admin-customers.html
  assets/css/wooten-statement-preview.css
  assets/js/wooten-statement-preview.mjs

Checked: button order, unique control IDs, JavaScript syntax, scoped desktop
and phone styles, and the existing preview behavior checks. The local browser could not launch, so the
updated layout has not been visually verified in a live browser here.
